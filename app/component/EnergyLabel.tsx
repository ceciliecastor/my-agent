export default function EnergyLabel({ label }) {
  return (
    <div>
      EnergyLabel hvis value === "A": bg-[#10AC84] hvis value === "B":
      bg-[#F2C94C] hvis value === "C": bg-[#F2994A] hvis value === "D":
      bg-[#EB5757]
    </div>
  );
}
